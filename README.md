# My dotfiles

These are some of the configurations for my linux system all stored in `~/.config/`.
- zsh
- dwm
- dmenu
- slock
- and more

`~/.local/bin/` contains scripts for a status bar, startup script, togglable grayscale for focus, etc.

`~/.local/share/backgrounds/` has my backgrounds
